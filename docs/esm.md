# ESM

Since 5.8.0, `angular-pipes` distributes ESM (ES6-module) files to allow tree shaking with a module bundler like `Rollup` or `Google's Closure Compiler`.

The esm's files are structured like the `src` folder, with `ngsummaries` for AoT. They can be found in the `esm` directory.